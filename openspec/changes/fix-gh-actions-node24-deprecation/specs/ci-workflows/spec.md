## ADDED Requirements

None — this change has no user-facing or behavior requirements. It is a version bump of CI action dependencies from Node.js 20 to Node.js 24 to resolve GitHub Actions deprecation warnings. Workflow behavior is unchanged.
